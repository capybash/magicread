{
  "private": true,
  "name": "@capybash-magicread",
  "scripts": {
    "prebuild": "rm -rf dist",
    "dev": "webpack --mode development --watch",
    "build": "webpack --mode production",
    "clean": "rm -rf dist",
    "format": "prettier --write src"
  },
  "devDependencies": {
    "flarum-webpack-config": "^1.0.0",
    "webpack": "^4.47.0",
    "webpack-cli": "^3.3.12",
    "@babel/runtime": "^7.24.0",
    "prettier": "^3.2.5"
  }
}